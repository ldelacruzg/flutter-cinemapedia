import 'package:flutter/material.dart';

class MoviesFavoritesView extends StatelessWidget {
  const MoviesFavoritesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Favorites View'),
      ),
      body: const Text('Favorites View'),
    );
  }
}
